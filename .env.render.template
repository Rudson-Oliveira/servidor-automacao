# ========================================
# VARIÁVEIS DE AMBIENTE - RENDER.COM
# Sistema COMETA - Servidor de Automação
# ========================================

# AMBIENTE
NODE_ENV=production
PORT=3000

# DATABASE
# Opção 1: Usar TiDB Cloud (atual)
DATABASE_URL=mysql://user:password@gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/database_name?ssl={"rejectUnauthorized":true}

# Opção 2: Usar PostgreSQL do Render
# DATABASE_URL=postgresql://user:password@hostname:5432/database_name

# AUTENTICAÇÃO
JWT_SECRET=<GERAR_AUTOMATICAMENTE_NO_RENDER>
VITE_APP_ID=<COPIAR_DO_MANUS_DASHBOARD>
OAUTH_SERVER_URL=https://api.manus.im
OWNER_OPEN_ID=<COPIAR_DO_MANUS_DASHBOARD>

# MANUS API
BUILT_IN_FORGE_API_URL=https://api.manus.im
BUILT_IN_FORGE_API_KEY=<COPIAR_DO_MANUS_DASHBOARD>

# EMAIL (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=<SEU_EMAIL_GMAIL>
SMTP_PASS=<SENHA_DE_APP_GMAIL>
SMTP_FROM=<SEU_EMAIL_GMAIL>

# DESKTOP AGENT
DESKTOP_AGENT_REGISTER_TOKEN=<GERAR_AUTOMATICAMENTE_NO_RENDER>

# FRONTEND (Vite)
VITE_FRONTEND_FORGE_API_KEY=<COPIAR_DO_MANUS_DASHBOARD>
VITE_FRONTEND_FORGE_API_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://portal.manus.im
VITE_APP_TITLE=Sistema COMETA
VITE_APP_LOGO=/logo.svg

# ANALYTICS (Opcional)
VITE_ANALYTICS_ENDPOINT=<OPCIONAL>
VITE_ANALYTICS_WEBSITE_ID=<OPCIONAL>

# ========================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ========================================

# 1. JWT_SECRET e DESKTOP_AGENT_REGISTER_TOKEN:
#    No Render.com, use "Generate Value" para criar automaticamente

# 2. Variáveis do Manus (VITE_APP_ID, OWNER_OPEN_ID, etc):
#    Copiar do dashboard do projeto atual em manus.im

# 3. SMTP (Email):
#    - Criar senha de app no Gmail: https://myaccount.google.com/apppasswords
#    - Usar essa senha em SMTP_PASS (não a senha normal)

# 4. DATABASE_URL:
#    - Manter TiDB Cloud atual OU
#    - Criar novo PostgreSQL no Render e usar connection string

# ========================================
# VERIFICAÇÃO DE SEGURANÇA
# ========================================

# ⚠️ NUNCA commitar este arquivo com valores reais
# ⚠️ Usar apenas no painel do Render.com
# ⚠️ Habilitar "Auto-Deploy" apenas após testar manualmente
